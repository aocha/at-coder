#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
typedef long long ll;

int main() {
  char c;
  cin >> c;
  if (isupper(c)) {
    cout << 'A' << endl;
  } else {
    cout << 'a' << endl;
  }
}
